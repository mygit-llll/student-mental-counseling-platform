<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zoctan.api.mapper.KnowledgeArticleMapper">
    <resultMap id="BaseResultMap" type="com.zoctan.api.entity.KnowledgeArticle">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="content" jdbcType="LONGVARCHAR" property="content"/>
        <result column="images" jdbcType="VARCHAR" property="images"/>
        <result column="category_id" jdbcType="BIGINT" property="categoryId"/>
        <result column="author_id" jdbcType="BIGINT" property="authorId"/>
        <result column="status" jdbcType="TINYINT" property="status"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="publish_time" jdbcType="TIMESTAMP" property="publishTime"/>
    </resultMap>

    <resultMap id="KnowledgeArticleDTOMap" type="com.zoctan.api.dto.KnowledgeArticleDTO">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="images" property="images"/>
        <result column="status" property="status"/>
        <result column="author_id" property="authorId"/>
        <result column="category_id" property="categoryId"/>
        <!-- 显式映射时间字段 -->
        <result column="create_time" property="createTime"/>
        <result column="publish_time" property="publishTime"/>
        <!-- 关联字段（SQL 中已用 AS 别名） -->
        <result column="authorName" property="authorName"/>
        <result column="categoryName" property="categoryName"/>
    </resultMap>

    <!-- 修改 select：使用 resultMap 而不是 resultType -->
    <select id="selectForAdminWithDetails" resultMap="KnowledgeArticleDTOMap">
        SELECT
        ka.id,
        ka.title,
        ka.content,
        ka.images,
        ka.status,
        ka.author_id,
        ka.category_id,
        ka.create_time,
        ka.publish_time,
        a.name AS authorName,
        kc.name AS categoryName
        FROM knowledge_article ka
        LEFT JOIN account a ON ka.author_id = a.id
        LEFT JOIN knowledge_category kc ON ka.category_id = kc.id
        <where>
            <if test="status != null">
                ka.status = #{status}
            </if>
            <if test="title != null and title != ''">
                AND ka.title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="authorId != null">
                AND ka.author_id = #{authorId}
            </if>
        </where>
        ORDER BY ka.create_time DESC
    </select>

</mapper>